# 🛸 Детекция дронов с помощью YOLO

Система для детекции дронов на изображениях, видео и в потоках с веб-камеры, основанная на модели YOLO.  
Поддерживает визуализацию в реальном времени, сохранение видео, отображение вероятности детекции (confidence), показ FPS и дообучение модели на новых данных.

---

## 🚀 Возможности

- Детекция дронов на изображениях, видео и с веб-камеры  
- Отрисовка bounding box, класса объекта и confidence  
- Опциональное отображение FPS  
- Сохранение обработанного видео  
- Выбор устройства для инференса: CPU или GPU

---

## 📦 Установка

### 1. Клонирование репозитория

```bash
git clone https://github.com/aibryanov/yolo_drone_detection/tree/main
cd yolo_drone_detection
```

### 2. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 3. Загрузка весов YOLOv8

```bash
python src/download_weights.py --url "https://drive.google.com/uc?id=1NMqusI1GbPrKljciz15CG-VvYlwiMqed"
```

## ▶️ Использование

### 🛠 Параметры командной строки

Скрипт `main.py` поддерживает следующие аргументы:

| Параметр | Тип | По умолчанию | Описание |
|-----------|-----|--------------|----------|
| `--source` | `str` | `webcam` | Источник данных: `webcam` для камеры, путь к видео (`video.mp4`) или изображению (`image.jpg`) |
| `--weights` | `str` | `models/best.pt` | Путь к весам модели YOLO |
| `--save` | `str` | `None` | Путь для сохранения обработанного видео. Если не указан, видео не сохраняется |
| `--showfps` | `flag` | `True` | Показать FPS на экране при обработке видео/кадров |
| `--no-showfps` | `flag` | `False` | Не показывать FPS (отключает `--showfps`) |
| `--device` | `str` | `auto` | Устройство для инференса: `cpu`, `cuda`. Если не указано, автоматически выбирается доступное устройство |

---

### 🎥 Детекция с веб-камеры

```bash
python main.py --source webcam --weights models/best.pt --showfps
```

### 🎥 Детекция из видеофайла

```bash
python main.py --source <путь_к_видео> --weights models/best.pt --save <путь_для_сохранения>
```

### 📷 Детекция из изображения

```bash
python main.py --source <путь_к_фото> --weights models/best.pt --save <путь_для_сохранения>
```

## Структура проекта
```
project/
│
├── src/                        # Исходный код проекта
│   ├── detection.py            # Класс YOLODetector и функции детекции
│   ├── download_weights.py     # Скрипт для скачивания весов модели
│   ├── utils.py                # Вспомогательные функции (FPS)
│   └── video_processor.py      # Класс для обработки видео и потоков
│
├── tests/                      # Тестовые изображения и видео
│
├── drone_detection.ipynb       # Jupyter Notebook с обучением YOLO
├── main.py                     # Основной скрипт запуска проекта
└── requirements.txt            # Зависимости проекта
```





